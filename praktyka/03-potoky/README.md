# Практична 03. Потоки і GIL

**Тиждень 3**

Дві версії, які суперечать одна одній.

**Що зробити**

1. Потокова версія задачі через `ThreadPoolExecutor`.
   Заміри для 2, 4, 8, `cpu_count()` потоків. Три прогони.
   Обов'язково `assert rezultat == base`.
2. Потокова версія задачі **з очікуванням**: 32 виклики
   по `time.sleep(0.5)`. Ті самі кількості потоків.
3. Один графік, **дві криві**: обчислення й очікування.
4. `vysnovky.md`, 5–7 речень: чому криві різні.

**Здати:** `2-potoky/`

**Критерії — 8 балів**

| Критерій | Балів |
|---|---|
| Обидві версії працюють, `assert` проходить | 2 |
| Заміри для всіх кількостей, три прогони | 2 |
| Графік із двома кривими | 2 |
| Пояснення через GIL і тип задачі | 2 |

⭐ Заміряйте те саме на NumPy-версії й покажіть, що там потоки дають приріст.
