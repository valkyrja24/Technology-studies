# Практична 04. Процеси і межа масштабування

**Тиждень 4**

Прискорення з'явиться. Але не в стільки разів, скільки ядер.

**Що зробити**

1. Версія через `ProcessPoolExecutor`. `assert` на збіг.
   ⚠️ `if __name__ == "__main__":` обов'язково.
2. Заміри для 1, 2, 4, 8, `cpu_count()` процесів. Три прогони.
3. Дізнайтесь **фізичну** кількість ядер, вкажіть окремо від логічної.
4. Графік із двома кривими: **прискорення** і **ефективність** S/N.
5. Заміряйте окремо вартість створення пулу:
   `with ProcessPoolExecutor(N) as ex: pass`
6. `vysnovky.md`: розкладіть розрив за трьома джерелами.

**Здати:** `3-protsesy/`

**Критерії — 8 балів**

| Критерій | Балів |
|---|---|
| Працює, `assert` проходить | 2 |
| Заміри для всіх кількостей, три прогони | 1 |
| Фізичні ядра вказані окремо від логічних | 1 |
| Графік прискорення й ефективності | 2 |
| Пояснення розриву за трьома джерелами, з числами | 2 |

⭐ Прогоніть для `chunksize` = 1, 8, 50, 200. Поясніть, чому крайні погані.

⚠️ Пояснення розриву **тільки** законом Амдала не приймається:
перевірте спершу, чи ваша послідовна частка це пояснює.
