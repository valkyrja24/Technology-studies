# РСПО 2026/2027 — репозиторій курсу

**Технології розподілених систем та паралельних обчислень**, 4 курс.

Натисніть **Use this template** → створиться ваша власна копія.
Далі працюєте тільки в ній.

---

## Головне правило курсу

> Одна задача проходить через увесь семестр і переписується шість разів.
> Щоразу ви **міряєте на власній машині** й **пояснюєте**, чому вийшло
> саме так.

Оцінюється не швидкість. Оцінюється **пояснення**.

---

## Структура

```
1-poslidovno/     послідовна реалізація
2-potoky/         потоками
3-protsesy/       процесами
4-numba/          NumPy та Numba
5-gpu/            GPU у Colab
6-vuzly/          кілька вузлів у контейнерах
praktyka/         решта практичних за темами
lektsii/          конспекти пройдених тем (додається по ходу семестру)
zamiry.md         ⭐ таблиця всіх вимірювань — головний файл
```

---

## ⚠️ Вимоги до кожного заміру

Без цього робота **не оцінюється вище 3 балів із 8**:

| Що вказати | Приклад |
|---|---|
| Процесор | Intel Core i5-3320M |
| Ядер фізичних / логічних | 2 / 4 |
| Розмір задачі | 1200×800, max_iter=300 |
| **Кількість прогонів** | 3 |
| Що входить у замір | обчислення + створення пулу |

Причина проста: **числа без умов непорівнянні**, а весь курс
побудований на порівнянні.

---

## Перевірка правильності

У кожній реалізації обов'язково:

```python
assert rezultat == base, "результат розійшовся з послідовним"
```

⚠️ Виняток — векторизована версія (практична 06). Там формула інша,
тому допускається розбіжність в одиничних точках на межі фрактала.
Її треба **порахувати й показати**, а не проігнорувати.

---

## Оцінювання

| Складова модуля | Балів |
|---|---|
| 7 практичних по 8 (зараховується не більше 50) | 50 |
| Проєкт модуля | 30 |
| Захист проєкту | 20 |
| **Разом за модуль** | **100** |

Два модулі, підсумкова — середнє. Іспит виставляється автоматом.

**Буфер:** 7 × 8 = 56 можливих, зараховується 50. Одне пропущене
заняття не псує оцінку, і довідки не потрібні.

**Шлях мінімуму:** хто не робив нічого — три питання з переліку
модуля усно, до 20 балів за питання, стеля 60 за модуль.

---

## Правило щодо AI

Використовувати не заборонено.

Заборонено **не вміти пояснити результат**. На захисті питання
починається з «скільки у вас ядер» і продовжується вашими числами.
Чужий графік не пройде: він не збігається з вашим `os.cpu_count()`.

---

## Git — мінімум, який потрібен

```bash
git clone <адреса вашої копії>
cd rspo-2026
git add .
git commit -m "практична 02: послідовна версія + профіль"
git push
```

Коміт після кожної практичної. Один коміт наприкінці семестру
виглядає саме так, як виглядає.

---

## Середовище

| Практичні | Що потрібно |
|---|---|
| 01–05 | чистий Python 3.12, **власна машина** |
| 06 | `pip install numpy numba` |
| 07 | браузер: Google Colab або NVIDIA DLI |
| 10–14 | Docker + Redis |
| 15 | акаунт Google |

⚠️ Практичні 01–05 міряються **на власній машині**, а не в хмарі:
безкоштовний Colab дає 2 ядра, і крива прискорення на ньому
не показує нічого.
